server:
  port: 8083

spring:
  application:
    name: bank-transactions-cliente

  datasource:
    url: jdbc:postgresql://localhost:5432/banco_connect_db
    username: jair123
    password: jair123
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect