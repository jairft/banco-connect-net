spring:
  application:
    name: bank-cliente-cadastro

  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true


eureka:
  client:
    serviceUrl:
        defaultZone: http://localhost:8761/eureka

server:
  port: 8081